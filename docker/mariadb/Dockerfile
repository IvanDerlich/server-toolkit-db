FROM mariadb:11

# Optional: Add custom configuration or entrypoint scripts here
# COPY my.cnf /etc/mysql/conf.d/
# COPY entrypoint.sh /usr/local/bin/entrypoint.sh
# RUN chmod +x /usr/local/bin/entrypoint.sh

# USER and permissions are set in docker-compose.yml for flexibility
